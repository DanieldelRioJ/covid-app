import { NumberFomatPipe } from './number-fomat.pipe';

describe('NumberFomatPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberFomatPipe();
    expect(pipe).toBeTruthy();
  });
});
